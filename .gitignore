# Keep the cleaning/QA engine private — it's the moat. Publish data + methodology only.
build_dataset.py
build_universe.py
pull_edgar.py
POST_DRAFT.md
data/pit_universe.csv
data/universe_progress.txt

__pycache__/
*.pyc
.DS_Store
.env

# Never publish the paid product or any variant of it. A .bak slipped past the
# bare-filename rules once and shipped 176k rows of the paid universe publicly.
data/pit_universe*
*.bak

# AI-tooling scaffolding — not part of the published sample. Quants are invited to audit
# this repo; 18 vendored image-generation skill files are noise at best, and the LICENSE
# would wrongly assert our copyright over third-party content.
.agents/
skills-lock.json
POST_DRAFT.md
__pycache__/
